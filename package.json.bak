# CMS Migration Commands

Add these scripts to your package.json:

```json
{
  "scripts": {
    "migrate-cms": "npx tsx scripts/migrate-products-to-cms.ts",
    "migrate-cms:live": "npx tsx scripts/migrate-products-to-cms.ts --live",
    "migrate-cms:rollback": "npx tsx scripts/migrate-products-to-cms.ts rollback"
  }
}